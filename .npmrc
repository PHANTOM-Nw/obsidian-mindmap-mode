# Obsidian installs a plugin from the GitHub release tagged exactly as
# manifest.json's version. npm's default prefix would make `npm version 1.0.2`
# tag v1.0.2, and the release would silently refuse to install.
tag-version-prefix=""
